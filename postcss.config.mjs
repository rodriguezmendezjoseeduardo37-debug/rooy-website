/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    // EN V4 SE USA ESTE PAQUETE, NO 'tailwindcss' A SECAS
    '@tailwindcss/postcss': {}, 
  },
};

export default config;